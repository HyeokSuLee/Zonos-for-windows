# Zonos-for-Windows 설계 안내

## 1. 프로젝트 개요

Zonos-for-Windows는 다국어 텍스트-음성 변환(TTS) 모델인 Zonos를 Windows 환경에서 실행할 수 있도록 포팅한 프로젝트입니다. 200,000시간 이상의 다국어 음성 데이터로 훈련된 이 모델은 전문적인 수준의 자연스러운 음성을 생성할 수 있으며, 짧은 오디오 샘플만으로도 음성 클로닝이 가능합니다.

### 주요 기능
- **제로샷 TTS와 음성 클로닝**: 원하는 텍스트와 10-30초 화자 샘플로 고품질 TTS 출력 생성
- **오디오 프리픽스 지원**: 더 풍부한 화자 매칭을 위한 텍스트와 오디오 프리픽스 입력
- **다국어 지원**: 영어, 일본어, 중국어, 프랑스어, 독일어, 한국어 등 100개 이상의 언어 지원
- **감정 및 음성 품질 제어**: 말하기 속도, 피치, 최대 주파수, 오디오 품질, 행복/슬픔/분노 등 다양한 감정 조정
- **Gradio 웹 인터페이스**: 사용하기 쉬운 그래픽 인터페이스 제공
- **멀티 화자 대화 생성**: 여러 화자의 대화를 자연스럽게 생성하는 기능
- **데이터 관리 기능**: 화자 오디오, 설정 프리셋, 대화 등을 사용자 지정 디렉토리에 저장 및 관리

## 2. 시스템 아키텍처

Zonos는 다음과 같은 핵심 컴포넌트로 구성되어 있습니다:

### 2.1 아키텍처 개요

```
텍스트 입력 → 텍스트 정규화 → eSpeak 음소화 → 조건부 처리 → 
트랜스포머/하이브리드 백본 → DAC 토큰 예측 → 오디오 디코딩 → 생성된 음성
```

![아키텍처 다이어그램](assets/ArchitectureDiagram.png)

### 2.2 주요 컴포넌트

#### 모델 (zonos/model.py)
- **Zonos 클래스**: 메인 모델 구현
- **오토인코더**: DAC(Discrete Audio Codec) 기반 오디오 인코딩/디코딩
- **백본**: 트랜스포머 또는 하이브리드(트랜스포머+Mamba) 신경망
- **샘플링 알고리즘**: 고품질 오디오 생성을 위한 특수 샘플링 기법

#### 조건부 처리 (zonos/conditioning.py)
- **PrefixConditioner**: 다양한 조건부 처리 통합 관리
- **EspeakPhonemeConditioner**: 텍스트를 음소로 변환
- **화자 임베딩**: 오디오 샘플에서 화자 특성 추출
- **감정 조절**: 8가지 감정 요소 조절 (행복, 슬픔, 혐오, 두려움, 놀람, 분노, 기타, 중립)
- **품질 조절**: VQ 스코어, 최대 주파수, 피치 표준편차, 말하기 속도, DNSMOS 조절

#### 사용자 인터페이스 (gradio_interface.py)
- **Gradio 웹 UI**: 직관적인 웹 기반 사용자 인터페이스
- **멀티 화자 대화 생성**: 여러 화자의 대화를 한 번에 생성
- **화자 및 설정 프리셋 관리**: 화자 및 설정 저장/불러오기 기능
- **대화 스크립트 관리**: 대화 스크립트 저장/불러오기 기능
- **데이터 저장 관리**: 사용자 지정 경로에 모든 데이터를 체계적으로 저장

## 3. 설치 및 요구 사항

### 3.1 시스템 요구 사항
- **운영 체제**: Windows 10/11
- **GPU**: NVIDIA GPU (최소 6GB VRAM)
  - 하이브리드 모델은 3000 시리즈 이상의 NVIDIA GPU 필요
- **CUDA**: CUDA 12.4
- **C++ 컴파일러**: Visual Studio 2022 (C++ 빌드 도구 포함)
- **Python**: Python 3.10 이상

### 3.2 설치 과정
1. **PowerShell 스크립트 실행 권한 설정** (관리자 권한으로 실행)
   ```powershell
   Set-ExecutionPolicy Unrestricted
   ```

2. **CUDA 12.4 설치**
   - https://developer.nvidia.com/cuda-12-4-1-download-archive?target_os=Windows&target_arch=x86_64

3. **Visual Studio 2022 설치**
   - https://visualstudio.microsoft.com/vs/
   - 설치 시 "C++를 사용한 데스크톱 개발" 워크로드 선택

4. **원클릭 설치 스크립트 실행**
   - `1、install-uv-qinglong.ps1` 파일을 마우스 우클릭 후 "PowerShell로 실행" 선택
   - 이 스크립트는 다음 작업을 수행합니다:
     - uv 패키지 관리자 설치
     - Python 가상 환경 생성
     - 의존성 패키지 설치
     - eSpeak-NG 음성 합성 엔진 설치

## 4. 사용 방법

### 4.1 실행 방법
1. **Gradio 인터페이스 실행**
   - `2、run_gradio.ps1` 파일을 마우스 우클릭 후 "PowerShell로 실행" 선택
   - 이 스크립트는 다음 작업을 수행합니다:
     - MSVC 환경 설정
     - Python 가상 환경 활성화
     - 허깅페이스 및 CUDA 환경 변수 설정
     - Gradio 웹 인터페이스 실행

2. **웹 브라우저에서 접속**
   - 기본 주소: http://127.0.0.1:7860

### 4.2 인터페이스 사용법

#### 데이터 저장 경로 설정
1. **저장 경로 지정**
   - 인터페이스 상단의 "데이터 저장 경로" 텍스트 박스에 원하는 경로 입력
   - "저장 경로 설정" 버튼 클릭
   - 이 경로에 화자 오디오, 프리셋, 대화 스크립트 등이 저장됨
   - 기본 경로: 사용자 홈 디렉토리 아래 "ZonosData" 폴더

#### 화자 관리
1. **화자 추가**
   - 화자 이름 입력
   - 화자 오디오 샘플 업로드 (10-30초 길이 권장)
   - "화자 추가" 버튼 클릭
   - 오디오 파일은 지정된 데이터 경로의 "speaker_audio" 폴더에 자동 복사됨

2. **화자 삭제**
   - 화자 목록에서 삭제할 화자 선택
   - "화자 삭제" 버튼 클릭

3. **화자 목록 저장/불러오기**
   - "화자 목록 저장" 버튼으로 현재 화자 목록 저장
   - "화자 목록 불러오기" 버튼으로 저장된 화자 목록 불러오기
   - 화자 목록은 데이터 경로에 "saved_speakers.json" 파일로 저장됨

#### 대화 입력
대화 입력은 다음 형식으로 작성합니다:

```
[화자명]: 텍스트
[화자명|설정1:값1|설정2:값2]: 텍스트
```

예시:
```
[Alice]: 안녕하세요, 만나서 반갑습니다.
[Bob|happy:0.5|sad:0.3|rate:20.0|cfg:2.5]: 네, 저도 만나서 반갑습니다.
[Alice|happy:0.9|pitch:60.0|seed:42]: 오늘 날씨가 정말 좋네요!
```

#### 지원하는 설정 옵션
- **감정 설정**: happy, sad, disgust, fear, surprise, anger, other, neutral (0.0~1.0)
- **음성 품질 설정**: vq (0.5~0.8), fmax (0~24000), pitch (0.0~300.0), rate (5.0~30.0), dnsmos (1.0~5.0)
- **시스템 설정**: noised (true/false), cfg (1.0~5.0), seed (정수)

#### 설정 프리셋 관리
1. **설정 저장**
   - 원하는 설정 조정
   - 프리셋 이름 입력
   - "현재 설정 저장" 버튼 클릭
   - 프리셋은 데이터 경로의 "presets" 폴더에 JSON 형식으로 저장됨

2. **설정 불러오기**
   - 저장된 프리셋 선택
   - "프리셋 불러오기" 버튼 클릭

3. **설정 삭제**
   - 삭제할 프리셋 선택
   - "프리셋 삭제" 버튼 클릭

#### 대화 스크립트 관리
1. **대화 저장**
   - 대화 텍스트 입력
   - 대화 이름 입력
   - "대화 저장" 버튼 클릭
   - 대화는 데이터 경로의 "saved_dialogues" 폴더에 텍스트 파일로 저장됨

2. **대화 불러오기**
   - 저장된 대화 선택
   - "대화 불러오기" 버튼 클릭

3. **대화 삭제**
   - 삭제할 대화 선택
   - "대화 삭제" 버튼 클릭

#### 오디오 생성
1. **오디오 생성**
   - 대화 입력 및 설정 조정 후
   - "오디오 생성" 버튼 클릭

2. **오디오 처리**
   - "오디오 정규화 적용" 체크박스: 생성된 오디오의 음량 정규화
   - "볼륨 조정 (dB)" 슬라이더: 출력 볼륨 조절

## 5. 핵심 구성 요소 상세 설명

### 5.1 데이터 저장 구조
```
사용자_지정_경로/
├── presets/                # 설정 프리셋 저장 폴더
│   └── 프리셋이름.json      # 프리셋 설정 파일
├── saved_dialogues/        # 저장된 대화 폴더
│   └── 대화이름.txt         # 대화 스크립트 파일
├── speaker_audio/          # 화자 오디오 파일 폴더
│   └── 화자이름.wav         # 화자별 오디오 파일
└── saved_speakers.json     # 화자 목록 및 경로 정보
```

### 5.2 Zonos 모델 구조 (zonos/model.py)

Zonos 클래스는 다음과 같은 주요 메서드를 제공합니다:

- **from_pretrained()**: 허깅페이스에서 사전 훈련된 모델 로드
- **make_speaker_embedding()**: 오디오 샘플에서 화자 임베딩 생성
- **prepare_conditioning()**: 조건부 입력 준비
- **generate()**: 조건부 입력을 바탕으로 오디오 생성

### 5.3 조건부 처리 (zonos/conditioning.py)

조건부 처리는 다음 클래스들로 구성됩니다:

- **PrefixConditioner**: 다양한 조건부 처리 통합
- **EspeakPhonemeConditioner**: 텍스트를 음소로 변환
- **FourierConditioner**: 연속적인 값(예: 감정, 피치)을 처리
- **IntegerConditioner**: 정수 값(예: 언어 ID) 처리
- **PassthroughConditioner**: 그대로 통과시키는 처리기

각 조건부 처리기는 특정 타입의 입력을 처리하고, PrefixConditioner가 이들을 통합하여 모델에 제공합니다.

### 5.4 Gradio 인터페이스 (gradio_interface.py)

Gradio 인터페이스는 다음과 같은 주요 기능을 제공합니다:

- **build_interface()**: 전체 UI 구성
- **load_model_if_needed()**: 필요시 모델 로드
- **generate_multi_speaker_audio()**: 여러 화자의 대화 생성
- **화자 관리 함수**: add_speaker(), remove_speaker(), save_speakers(), load_speakers()
- **설정 관리 함수**: save_settings_preset(), load_settings_preset(), delete_settings_preset()
- **대화 관리 함수**: save_dialogue(), load_dialogue(), delete_dialogue()
- **데이터 경로 관리**: update_data_directory() - 사용자 지정 경로 설정

### 5.5 멀티 화자 대화 처리 로직

1. **대화 파싱**: parse_dialogue() 함수로 대화 텍스트 파싱
2. **청크 분할**: split_into_chunks() 함수로 텍스트를 적절한 크기로 분할
3. **화자별 설정 적용**: 각 화자별 설정을 적용하여 오디오 생성
4. **오디오 처리**: 생성된 오디오 청크의 음량 조정 및 정규화
5. **오디오 결합**: 모든 오디오 청크를 하나로 결합하여 최종 출력 생성

## 6. 모델 성능 및 한계

### 6.1 성능 특성
- **생성 속도**: RTX 4090에서 약 2배의 실시간 속도로 생성 (2초 오디오 생성에 1초 소요)
- **출력 품질**: 44kHz 샘플링 레이트로 고품질 오디오 생성
- **모델 크기**: 트랜스포머 모델 약 1GB, 하이브리드 모델 약 1.2GB

### 6.2 한계 및 제약 사항
- **하드웨어 요구 사항**: 최소 6GB VRAM 필요
- **하이브리드 모델 제약**: 3000 시리즈 이상의 NVIDIA GPU 필요
- **CPU 실행**: CPU에서도 실행 가능하지만 속도가 매우 느림
- **메모리 사용량**: 긴 오디오 생성 시 상당한 메모리 필요
- **설정 직관성**: 감정 및 음성 품질 설정이 직관적이지 않을 수 있음

## 7. 문제 해결 및 팁

### 7.1 일반적인 문제 해결
- **CUDA 오류**: CUDA 버전(12.4) 확인 및 GPU 드라이버 업데이트
- **eSpeak 오류**: eSpeak-NG가 올바르게 설치되었는지 확인
- **메모리 부족**: 배치 크기 감소 또는 더 짧은 오디오 생성
- **음성 품질 문제**: VQ 스코어, DNSMOS, 기타 설정 조정
- **파일 액세스 오류**: 데이터 저장 경로에 쓰기 권한이 있는지 확인

### 7.2 최적화 팁
- **화자 임베딩**: 10-30초 길이의 깨끗한 오디오 샘플 사용
- **감정 조절**: 감정 값의 총합은 1.0에 가까워야 함
- **오디오 프리픽스**: 특정 스타일(예: 속삭임)을 위해 오디오 프리픽스 활용
- **설정 프리셋**: 자주 사용하는 설정을 프리셋으로 저장하여 재사용
- **데이터 경로**: 국문/특수문자가 없는 경로 사용 권장

### 7.3 대화 스크립트 작성 팁
- **화자별 설정**: 각 화자마다 다른 감정과 음성 특성 설정
- **텍스트 분할**: 너무 긴 텍스트는 자동으로 분할되나, 적절한 길이로 입력 권장
- **시드 값**: 동일한 출력을 위해 시드 값 고정 가능

## 8. 참고 자료

### 8.1 코드 구조
```
Zonos-for-windows/
├── zonos/                       # 핵심 모델 코드
│   ├── model.py                 # 메인 Zonos 모델 클래스
│   ├── conditioning.py          # 조건부 처리 클래스 및 함수
│   ├── autoencoder.py           # DAC 오토인코더
│   ├── backbone/                # 모델 백본 구현
│   ├── sampling.py              # 샘플링 알고리즘
│   └── utils.py                 # 유틸리티 함수
├── 1、install-uv-qinglong.ps1   # 설치 스크립트
├── 2、run_gradio.ps1            # 실행 스크립트
├── gradio_interface.py          # Gradio UI 인터페이스
├── assets/                      # 이미지 및 기본 오디오 파일
└── 설계 안내.md                  # 본 설계 안내 문서
```

### 8.2 관련 링크
- 원본 Zonos 프로젝트: https://github.com/Zyphra/Zonos
- Zyphra 홈페이지: https://www.zyphra.com
- Zonos 블로그 포스트: https://www.zyphra.com/post/beta-release-of-zonos-v0-1
- Zonos 온라인 데모: https://playground.zyphra.com/audio

---

본 설계 안내서는 Zonos-for-Windows 프로젝트의 구조와 사용법을 이해하는 데 도움이 됩니다. 더 자세한 정보는 프로젝트 내의 README.md 및 기타 문서를 참조하십시오.
